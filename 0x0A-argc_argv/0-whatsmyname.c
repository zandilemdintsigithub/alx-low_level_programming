#include "main.h"
#include <stdio.h>

/**
 * main - print name followed by new line
 * @argc: arg count
 * @argv: arg vector
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
